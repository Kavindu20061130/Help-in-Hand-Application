Dear Lecturer,

To run the file, first install Java on the computer.
Next, open the terminal inside the project folder and run:

./mvnw spring-boot:run


After the application starts, open Google Chrome and go to:

http://localhost:8080


The project will run successfully.